<%= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :item_category %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :item_title %>
  </div>

  <div class="field">
    <%= form.label :width %>
    <%= form.text_field :width, id: :item_width %>
  </div>

  <div class="field">
    <%= form.label :length %>
    <%= form.text_field :length, id: :item_length %>
  </div>

  <div class="field">
    <%= form.label :height %>
    <%= form.text_field :height, id: :item_height %>
  </div>

  <div class="field">
    <%= form.label :cubic_weight %>
    <%= form.text_field :cubic_weight, id: :item_cubic_weight %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
